/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package cyclonedx.test.app;

import cyclonedx.test.list.LinkedList;

import static cyclonedx.test.utilities.StringUtils.join;
import static cyclonedx.test.utilities.StringUtils.split;
import static cyclonedx.test.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
